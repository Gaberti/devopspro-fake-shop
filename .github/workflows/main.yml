name: fakeshop-cicd
on:
  push:
    branches: ["main"]
  workflow_dispatch:


jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - name: Obter Codigo
        uses: actions/checkout@v4.2.2
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Construcao e envio docker image
        uses: docker/build-push-action@v6
        with:
          context: ./src
          push: true
          file: ./src/Dockerfile
          tags: gabberti/devopspro-fake-shop:v2
      - run: echo "Enviar para Docker Hub"
  CD:
    needs: [CI]
    runs-on: ubuntu-latest
    steps:
      - run: echo "Obter código"
      - run: echo "Configurar kubeconfig"
      - run: echo "Apply"
